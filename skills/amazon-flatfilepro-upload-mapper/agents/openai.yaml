interface:
  display_name: "FlatFilePro Upload Mapper"
  short_description: "Map FlatFilePro upload columns in Chrome."
  default_prompt: "Use this skill when a prepared FlatFilePro CSV needs to be uploaded in Chrome, matched by SKU, mapped column-by-column, checked for validation issues, and left at the final confirmation step."
