interface:
  display_name: "Amazon Account Health Check"
  short_description: "Daily account health action queue with dispositions"
  default_prompt: "Use $amazon-account-health-check to run today's account health queue and dispose every open finding."
