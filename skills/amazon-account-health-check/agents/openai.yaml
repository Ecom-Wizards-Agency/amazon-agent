display_name: Amazon Account Health Check
short_description: Run daily Seller Central account health checks with configurable alerts, posts, and follow-ups.
default_prompt: Run the daily Amazon account health check for the configured accounts, post the parent thread and account comments to the configured destination when requested, and create follow-up tasks only when a task tracker is configured.
