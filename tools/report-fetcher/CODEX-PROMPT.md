# Legacy compatibility path

This agent-named filename is retained so existing links do not break. The canonical,
agent-neutral prompt is [`BROWSER-PROMPT.md`](BROWSER-PROMPT.md). Use that file for every new run.
